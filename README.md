Sources for the plugin `CoronaProvider.analytics.flurry`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.analytics.flurry" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
